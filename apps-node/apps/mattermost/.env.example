# Mattermost stack environment (apps-node). Paste into the Portainer stack env
# or use as apps-node/apps/mattermost/.env with `make app-up NAME=mattermost`.

PRIMARY_DOMAIN=meenerva.io
TZ=UTC

CORE_DB_HOST=10.10.0.1
CORE_DB_PORT=5432
CORE_SMTP_HOST=10.10.0.1
CORE_SMTP_PORT=587
MAIL_FROM=no-reply@meenerva.io
MAIL_FROM_PASSWORD=CHANGE_ME

MATTERMOST_DB_PASSWORD=CHANGE_ME     # from ./scripts/create-app-database.sh mattermost
